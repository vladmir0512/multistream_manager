# Stream Manager для OBS Studio

**Панель управления названием и категорией стрима на Twitch, YouTube, Trovo, VK Play Live и Kick одновременно.**

---

## 📋 Содержание
1. [Структура проекта](#структура-проекта)
2. [Требования](#требования)
3. [Установка](#установка)
4. [Получение токенов для каждой платформы](#получение-токенов)
5. [Запуск](#запуск)
6. [Добавление в OBS](#добавление-в-obs)
7. [Использование](#использование)

---

## 📁 Структура проекта

```
stream-manager/
├── app.py                 # Основной Flask приложение
├── templates/
│   └── index.html        # Интерфейс панели
├── stream_history.json   # История стримов (создаётся автоматически)
├── requirements.txt      # Зависимости Python
├── .env                  # Переменные окружения (ВАЖНО: добавить в .gitignore)
└── README.md             # Этот файл
```

---

## 🔧 Требования

- **Python 3.8+**
- **pip** (менеджер пакетов Python)
- **OBS Studio** (любая версия, поддерживающая браузерные доки)
- Интернет-соединение
- Токены для платформ (см. ниже)

---

## 💾 Установка

### Шаг 1: Клонирование/загрузка файлов

```bash
# Если используете Git
git clone https://your-repo-url.git
cd stream-manager

# Или просто создайте папку и положите файлы туда
```

### Шаг 2: Установка зависимостей

```bash
# Windows
python -m pip install -r requirements.txt

# macOS / Linux
python3 -m pip install -r requirements.txt
```

### Шаг 3: Создание файла .env

Создайте файл `.env` в корне проекта и добавьте туда свои токены:

```env
# TWITCH
TWITCH_CLIENT_ID=your_client_id_here
TWITCH_TOKEN=your_access_token_here
TWITCH_BROADCASTER_ID=your_broadcaster_id_here

# YOUTUBE
YOUTUBE_TOKEN=your_youtube_oauth_token_here
YOUTUBE_VIDEO_ID=your_broadcast_video_id_here

# TROVO
TROVO_CLIENT_ID=your_trovo_client_id_here
TROVO_TOKEN=your_trovo_access_token_here
TROVO_CHANNEL_ID=your_channel_id_here

# VK PLAY LIVE
VKPLAY_TOKEN=your_vkplay_token_here
VKPLAY_CHANNEL_ID=your_channel_id_here

# KICK
KICK_TOKEN=your_kick_token_here
KICK_CHANNEL_SLUG=your_channel_name_here
```

> **⚠️ ВАЖНО**: Не коммитьте файл `.env` в Git! Добавьте его в `.gitignore`:
> ```
> .env
> *.pyc
> __pycache__/
> stream_history.json
> ```

---

## 🔐 Получение токенов

### 1️⃣ Twitch

**Для получения Client ID и токена:**

1. Перейдите на https://dev.twitch.tv/console/apps
2. Создайте новое приложение:
   - **Name**: например "OBS Stream Manager"
   - **OAuth Redirect URL**: `http://localhost:5000/` (или `http://127.0.0.1:5000/`)
   - **Category**: Application Integration
3. Нажмите "Create"
4. В разделе "Client ID" скопируйте **Client ID**

**Для получения OAuth токена:**

1. Используйте https://twitchapps.com/tokengen/
2. Нажмите на окно консоли браузера → введите ваш Client ID
3. Авторизуйтесь
4. Выберите scopes:
   - ✅ `channel:manage:broadcast` (обязателен)
   - Остальное — опционально
5. Разрешите доступ
6. **Access Token** будет показан в URL (после `access_token=`)

**Для получения Broadcaster ID:**

```bash
curl -H "Client-ID: YOUR_CLIENT_ID" -H "Authorization: Bearer YOUR_TOKEN" \
  https://api.twitch.tv/helix/users
```

Ищите в ответе поле `id`.

---

### 2️⃣ YouTube

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите **YouTube Data API v3**:
   - Меню → APIs & Services → Library
   - Ищите "YouTube Data API v3"
   - Нажмите "Enable"
4. Создайте OAuth 2.0 Credentials:
   - APIs & Services → Credentials
   - Create Credentials → OAuth 2.0 Client ID
   - Application type: Desktop (или Web)
   - Добавьте Redirect URI: `http://localhost:5000/callback`
5. Скачайте JSON и получите токен через OAuth 2.0 flow

**Для получения Video ID текущей трансляции:**

```bash
curl -H "Authorization: Bearer YOUR_YOUTUBE_TOKEN" \
  https://www.googleapis.com/youtube/v3/liveBroadcasts?part=snippet\&broadcastStatus=active
```

Ищите `id` в ответе.

---

### 3️⃣ Trovo

1. Перейдите на https://developer.trovo.live/
2. Создайте приложение в разделе "Developer Applications"
3. Заполните:
   - **Application Name**: "OBS Stream Manager"
   - **Redirect URL**: `http://localhost:5000/`
   - **Terms & Privacy**: добавьте ссылки
4. Получите **Client ID** и **Client Secret**
5. Для получения токена используйте OAuth:
   ```
   https://open.trovo.live/page/login.html?client_id=YOUR_CLIENT_ID&response_type=token&scope=channel_details_self+channel_update_self&redirect_uri=http://localhost:5000/
   ```
6. После авторизации токен будет в URL (параметр `access_token=`)

**Для получения Channel ID:**

```bash
curl -H "Client-ID: YOUR_CLIENT_ID" \
  -H "Authorization: OAuth YOUR_TOKEN" \
  -X GET https://open-api.trovo.live/openplatform/getuserinfo
```

Ищите `channelId` в ответе.

---

### 4️⃣ VK Play Live

VK Play Live использует более сложную интеграцию. Рекомендуется:

1. Получить OAuth токен из VK Developer Console: https://dev.vk.com/
2. Использовать Chat Client для интеграции (требует дополнительной работы)
3. Текущая реализация поддерживает базовый REST API (если доступен)

> **Примечание**: Для полной поддержки VK Play Live может потребоваться дополнительная разработка.

---

### 5️⃣ Kick

Kick недавно запустил Public API. Получение токена:

1. Перейдите на https://developers.kick.com/ (когда будет доступен)
2. Создайте приложение
3. Получите **Access Token**
4. **Channel Slug** — это имя вашего канала (последняя часть URL: kick.com/YOUR_CHANNEL)

> **Примечание**: Kick API всё ещё развивается. Функциональность может быть ограничена.

---

## 🚀 Запуск

### Способ 1: Напрямую (для разработки)

```bash
python app.py
```

Приложение запустится на `http://localhost:5000`

### Способ 2: Через gunicorn (для production)

```bash
pip install gunicorn
gunicorn -w 1 -b 0.0.0.0:5000 app:app
```

---

## 📺 Добавление в OBS

### Шаг 1: Убедитесь, что сервер запущен

```bash
python app.py
# Ждите сообщения: "Running on http://0.0.0.0:5000"
```

### Шаг 2: Добавьте док-панель в OBS

1. **OBS Studio → Вид → Док-панели → Пользовательская браузерная док-панель**
2. Заполните форму:
   - **Название**: `Stream Manager` (или любое другое)
   - **URL**: `http://127.0.0.1:5000/`
3. Нажмите **Применить**

### Шаг 3: Переместите док

Док появится в главном окне OBS. Его можно:
- Перемещать (зажать по названию)
- Прикреплять к краям окна
- Отсоединять в отдельное окно

---

## 💡 Использование

### Основной workflow

1. **Введите название стрима** в поле "Название стрима"
   - Пример: `GTA V | Стрим #42 | Прямая трансляция 24/7`

2. **Введите категорию/игру** (опционально)
   - Пример: `GTA V` или `Just Chatting`

3. **Выберите платформы**, на которых хотите обновить:
   - ☑️ **Twitch** — включен по умолчанию
   - ☐ **YouTube**
   - ☐ **Trovo**
   - ☐ **VK Play**
   - ☐ **Kick**

4. **Нажмите "✓ Обновить"**
   - Если всё настроено правильно, увидите сообщение ✅
   - Если ошибка — проверьте консоль приложения

5. **История** автоматически сохраняется
   - Кликните на любой прошлый стрим, чтобы быстро восстановить

### Горячие клавиши

- **Enter** в поле названия: обновить (если внедрить в JS)
- **Ctrl+A** → скопировать историю в буфер обмена

### Продвинутые операции

**Группировка платформ по типам:**
- Gaming: Twitch, YouTube, Kick
- Community: Trovo, VK Play Live

**Использование переменных:**
Если хотите автоматически подставлять время/номер стрима, отредактируйте `app.py`:

```python
def add_timestamp_to_title(title):
    from datetime import datetime
    timestamp = datetime.now().strftime("%H:%M")
    return f"{title} | {timestamp}"
```

---

## 🐛 Troubleshooting

### Проблема: "Connection refused" при добавлении в OBS

**Решение:**
- Убедитесь, что сервер запущен (`python app.py`)
- Используйте `http://127.0.0.1:5000/` вместо `localhost`
- Проверьте порт 5000 (может быть занят)

### Проблема: "Unauthorized" или "Invalid token"

**Решение:**
- Проверьте токены в файле `.env`
- Убедитесь, что токены не истекли
- Для Twitch: токен живёт ~2 месяца
- Для YouTube: может потребоваться обновление через refresh token

### Проблема: Статус "Категория не найдена"

**Решение:**
- Проверьте правильность названия категории
- Используйте официальное имя (например, "Just Chatting", а не "just chat")
- Для Twitch: категория может отличаться

### Проблема: Ошибка CORS при запросе

**Решение:**
- Это нормально для браузерного дока OBS
- OBS браузер не применяет CORS, поэтому запросы должны работать

---

## 📊 Логирование

Все запросы логируются в консоль где запущен `python app.py`:

```
[TWITCH] Обновляю: My Stream Title | GTA V
[YOUTUBE] Обновляю: My Stream Title | GTA V
[TROVO] Error getting game ID: ...
```

Для сохранения логов в файл добавьте в `app.py`:

```python
import logging
logging.basicConfig(filename='stream_manager.log', level=logging.DEBUG)
```

---

## 🔐 Безопасность

### Лучшие практики

1. **Никогда не коммитьте `.env`**
   - Добавьте в `.gitignore`
   - Используйте шаблон `.env.example`

2. **Ограничьте токены минимально необходимыми scope**
   - Twitch: только `channel:manage:broadcast`
   - YouTube: только `youtube.manage`
   - Трово: только `channel_update_self`

3. **Используйте переменные окружения**
   - Не вставляйте токены в код
   - На сервере используйте системные переменные

4. **Регулярно проверяйте активные токены**
   - Twitch Console: https://dev.twitch.tv/console/apps
   - YouTube: Google Security Checkup
   - Удаляйте неиспользуемые токены

---

## 🚀 Развертывание (для production)

### Вариант 1: На локальной машине (Windows Service)

```powershell
# Создать batch-файл start_stream_manager.bat
@echo off
cd C:\path\to\stream-manager
python app.py
pause
```

Затем создать Windows Service через NSSM.

### Вариант 2: На сервере (Linux/Docker)

```dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["gunicorn", "-w", "1", "-b", "0.0.0.0:5000", "app:app"]
```

```bash
docker build -t stream-manager .
docker run -p 5000:5000 -e TWITCH_TOKEN=xxx stream-manager
```

---

## 📝 Лицензия

MIT License - используйте свободно!

---

## 🤝 Контрибьютинг

Если нашли баг или хотите добавить функцию:

1. Форкните репозиторий
2. Создайте ветку `feature/your-feature`
3. Отправьте Pull Request

**Идеи для развития:**
- [ ] Поддержка Facebook Gaming/Instagram Live
- [ ] Автоматическое обновление по расписанию
- [ ] Интеграция с SLOBS (Streamlabs OBS)
- [ ] Веб-интерфейс для удалённого управления
- [ ] Telegram бот для управления из телефона
- [ ] Настройка шаблонов названий

---

## 📞 Поддержка

Если возникли вопросы:
- Проверьте раздел [Troubleshooting](#troubleshooting)
- Посмотрите логи приложения
- Создайте Issue на GitHub

**Удачи с трансляциями! 🎬**